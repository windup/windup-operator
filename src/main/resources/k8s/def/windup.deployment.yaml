apiVersion: apps/v1
kind: Deployment
metadata:
  name: windup-operator
  namespace: windup
  labels:
      application: windup
spec:
  selector:
    matchLabels:
      app: windup-operator
  replicas: 1
  template:
    metadata:
      labels:
        app: windup-operator
    spec:
      serviceAccountName: windup-operator
      containers:
        - image: quay.io/windupeng/windup-operator-native:latest
          name: windup-operator
          imagePullPolicy: Always
          env:
          - name: WATCH_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          - name: RELATED_IMAGE_MTR_WEB
            value: {{ mtr_web_pullspec }}
          - name: RELATED_IMAGE_MTR_WEB_EXECUTOR
            value: {{ mtr_web_executor_pullspec }}
          - name: RELATED_IMAGE_POSTGRESQL
            value: {{ postgresql_pullspec }}
          resources:
            limits:
              cpu: 1
              memory: 200Mi
            requests:
              cpu: 100m
              memory: 50Mi
